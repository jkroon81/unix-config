general {
    colors = true
    interval = 5
}
order += "battery all"
order += "load"
order += "tztime local"
battery all {
    format = "%percentage %remaining"
}
tztime local {
    format = "%Y-%m-%d %H:%M"
}
load {
    format = "%1min"
}
